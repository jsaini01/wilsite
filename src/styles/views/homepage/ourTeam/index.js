import styled from "styled-components";

export const OurTeamWraper = styled.div`
  margin: 18.5vw 0 10vw 0;
`;

export const ContentWraper = styled.div`
  text-align: center;
`;

export const ContentHeading = styled.p`
  color: rgba(130, 130, 130, 1);
  font-weight: 400;
  font-size: clamp(0.5rem, 1.4vw, 1.5rem);
`;

export const MainHeading = styled.h1`
  font-size: clamp(2rem, 3.7vw, 4.5rem);
  height: clamp(1.6rem, 3.7vw, 4.5rem);
  line-height: 6vw;
`;

export const BorderWrap = styled.div`
  background-color: rgba(249, 197, 28, 1);
  height: clamp(0.5rem, 1vw, 1.5rem);
  width: clamp(10rem, 18.5vw, 20rem);
  margin: auto;
`;

export const Content = styled.p`
  font-size: clamp(0.5rem, 1.4vw, 2rem);
  text-transform: capitalize;
  font-weight: 700;
  line-height: 6.5vw;
`;

// img

export const ImageWraper = styled.div`
  display: flex;
  justify-content: center;
  margin: 2.8vw 0;
  position: relative;
`;

export const BackImg = styled.img`
  width: calc(100% - 31.97%);
`;

export const Animator = styled.img`
  position: absolute;
  right: clamp(20rem, 42.5vw, 60rem);
  top: clamp(0.2rem, 0.4vw, 2rem);
  width: calc(100% - 90.7%);
`;

export const AnimatorContent = styled.p`
  position: absolute;
  right: clamp(21rem, 44vw, 60rem);
  top: clamp(2rem, 4.2vw, 5rem);
  font-size: clamp(0.7rem, 1.4vw, 2rem);
  color: #ffffff;
  font-weight: 600;
`;

export const Ellipse = styled.img`
  position: absolute;
  right: clamp(14rem, 29.7vw, 60rem);
  top: clamp(2.3rem, 3.7vw, 10rem);
  width: calc(100% - 89.4%);
`;

export const JuliaCenter = styled.img`
  position: absolute;
  right: clamp(16.65rem, 33.8vw, 60rem);
  top: clamp(5.4rem, 11vw, 30rem);
  width: calc(100% - 72.1%);
`;

export const JuliaName = styled.h4`
  position: absolute;
  right: clamp(19.65rem, 41.6vw, 43rem);
  top: clamp(5.4rem, 27vw, 30rem);
  font-weight: 700;
  font-size: clamp(0.9rem, 1.7vw, 3rem);
  width: calc(100% - 88%);
  text-align: center;
  color: #ffffff;
`;

export const JuliaDescription = styled.p`
  position: absolute;
  top: clamp(1rem, 29.3vw, 30rem);
  left: clamp(10rem, 47.5vw, 50rem);
  font-weight: 400;
  font-size: clamp(0.5rem, 1.15vw, 2rem);
  text-align: center;
  width: calc(100% - 92%);
  color: #ffffff;
`;

export const JuliaContent = styled.p`
  position: absolute;
  top: clamp(1rem, 31.2vw, 40rem);
  left: clamp(20rem, 43.5vw, 50rem);
  font-size: clamp(0.4rem, 0.87vw, 2rem);
  text-align: center;
  width: calc(100% - 84%);
  color: #ffffff;
  line-height: 1.2vw;
`;

export const Employee200 = styled.img`
  position: absolute;
  right: clamp(10.8rem, 23.8vw, 60rem);
  top: clamp(7.3rem, 14.1vw, 20rem);
  width: calc(100% - 92.15%);
`;

export const EmployeContent = styled.p`
  position: absolute;
  right: clamp(10.8rem, 25.2vw, 60rem);
  top: clamp(8.5rem, 16.5vw, 20rem);
  font-weight: 600;
  font-size: clamp(0.5rem, 1.85vw, 2rem);
  text-align: center;
  width: calc(100% - 94.5%);
`;
export const EmployeSpan = styled.p`
  position: absolute;
  right: clamp(0rem, 0vw, 60rem);
  top: clamp(0rem, 2vw, 20rem);
  font-weight: 600;
  font-size: clamp(0.4rem, 1vw, 2rem);
  text-align: center;
`;

export const Ellipse1934 = styled.img`
  position: absolute;
  right: clamp(6rem, 12.8vw, 20rem);
  top: clamp(11.36rem, 22.2vw, 30rem);
  width: calc(100% - 87.3%);
`;

export const Ellipse1935 = styled.img`
  position: absolute;
  right: clamp(11.5rem, 25vw, 30rem);
  top: clamp(16.7rem, 34.7vw, 40rem);
  width: calc(100% - 87.57%);
`;

export const UxDesign = styled.img`
  position: absolute;
  right: clamp(19.1rem, 42.8vw, 50rem);
  top: clamp(19.81rem, 41vw, 50rem);
  width: calc(100% - 90.9%);
`;

export const UxContent = styled.p`
  position: absolute;
  right: clamp(19.1rem, 44vw, 50rem);
  top: clamp(19.81rem, 42.5vw, 50rem);
  text-align: center;
  width: calc(100% - 93.02%);
  font-size: clamp(1rem, 3vw, 4rem);
  font-weight: 600;
  color: #ffffff;
`;

export const UxSpan = styled.span`
  text-align: center;

  font-size: clamp(0.5rem, 1.1vw, 4rem);
  font-weight: 500;
  position: absolute;
  top: clamp(2rem, 3.6vw, 6rem);
  right: clamp(0rem, 0.6vw, 5rem);
`;

export const Ellipse1940 = styled.img`
  position: absolute;
  left: clamp(15rem, 31.9vw, 50rem);
  top: clamp(16.3rem, 35.45vw, 50rem);
  width: calc(100% - 88.6%);
`;

export const Founder = styled.img`
  position: absolute;
  left: clamp(10.3rem, 20.5vw, 30rem);
  top: clamp(18rem, 38vw, 42rem);
  width: calc(100% - 91.25%);
`;

export const FounderContent = styled.p`
  position: absolute;
  text-align: center;
  left: clamp(11.3rem, 22.6vw, 30rem);
  top: clamp(19rem, 40vw, 45rem);
  font-size: clamp(1.5rem, 2.94vw, 3rem);
  font-weight: 600;
  width: calc(100% - 95.5%);
`;

export const FounderSpan = styled.p`
  font-weight: 500;
  font-size: clamp(0.5rem, 1vw, 2rem);
  position: absolute;
  top: clamp(0rem, 3.2vw, 4rem);
`;

export const Ellipse1939 = styled.img`
  position: absolute;
  left: clamp(11.1rem, 22.9vw, 50rem);
  top: clamp(10.6rem, 22vw, 50rem);
  width: calc(100% - 86.95%);
`;

export const Devloper = styled.img`
  position: absolute;
  left: clamp(6rem, 13.5vw, 50rem);
  top: clamp(8rem, 18.2vw, 50rem);
  width: calc(100% - 90.77%);
`;

export const DevloperContent = styled.p`
  position: absolute;
  left: clamp(6rem, 14.5vw, 50rem);
  top: clamp(10.6rem, 21.9vw, 50rem);
  font-size: clamp(0.5rem, 1.45vw, 3rem);
  font-weight: 600;
  color: #ffffff;
`;

export const Ellipse1938 = styled.img`
  position: absolute;
  left: clamp(9.32rem, 19.2vw, 50rem);
  top: clamp(2rem, 5.3vw, 50rem);
  width: calc(100% - 86.02%);
`;

export const Ellipse1936 = styled.img`
  position: absolute;
  left: clamp(9.32rem, 35.8vw, 50rem);
  top: clamp(1.5rem, 3.7vw, 50rem);
  width: calc(100% - 92.44%);
`;
